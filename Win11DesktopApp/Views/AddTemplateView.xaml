<UserControl x:Class="Win11DesktopApp.Views.AddTemplateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="600">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ContentBackgroundBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource SecondaryForegroundBrush}"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </UserControl.Resources>
    
    <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="12" Width="500" Height="550">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="0" Color="#000000" Opacity="0.3"/>
        </Border.Effect>
        
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="{DynamicResource HeaderAddTemplate}" FontSize="{DynamicResource FS24}" FontWeight="Bold" Margin="0,0,0,20" Foreground="{DynamicResource ForegroundBrush}"/>
            
            <!-- Form -->
            <StackPanel Grid.Row="1">
                <!-- Name -->
                <TextBlock Text="{DynamicResource LabelTemplateName}"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                
                <!-- Format -->
                <TextBlock Text="{DynamicResource LabelFormat}"/>
                <ComboBox SelectedItem="{Binding SelectedFormat}" Margin="0,0,0,15" Height="35" VerticalContentAlignment="Center">
                    <sys:String>DOCX</sys:String>
                    <sys:String>XLSX</sys:String>
                    <sys:String>PDF</sys:String>
                </ComboBox>

                <!-- Description -->
                <TextBlock Text="{DynamicResource LabelDescription}"/>
                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Height="80" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

                <!-- File Upload -->
                <Button Command="{Binding UploadFileCommand}" Content="{DynamicResource BtnUploadFile}" 
                        HorizontalAlignment="Left" Padding="15,8" Margin="0,10,0,5"
                        IsEnabled="{Binding IsFileRequired}"/>
                
                <!-- DOCX info: file not needed -->
                <TextBlock Text="{DynamicResource TplFileNotNeeded}" 
                           FontSize="{DynamicResource FS12}" FontWeight="Normal" Foreground="{DynamicResource AccentBrush}" 
                           Margin="0,0,0,15"
                           Visibility="{Binding IsDocxFormat, Converter={StaticResource BoolToVis}}"/>

                <!-- Uploaded file name for XLSX/PDF -->
                <TextBlock Text="{Binding UploadedFileName, StringFormat={}{0}}" 
                           Visibility="{Binding IsFileUploaded, Converter={StaticResource BoolToVis}}"
                           Foreground="Green" Margin="0,0,0,15"/>
            </StackPanel>
            
            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Command="{Binding CancelCommand}" Content="{DynamicResource BtnCancel}" Width="100" Height="35" Margin="0,0,10,0" Background="Transparent" BorderBrush="{DynamicResource DividerBrush}" BorderThickness="1"/>
                <Button Command="{Binding SaveCommand}" Content="{DynamicResource BtnSaveTemplate}" Width="150" Height="35" Background="{DynamicResource AccentBrush}" Foreground="White" BorderThickness="0"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
