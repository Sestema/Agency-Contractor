<UserControl x:Class="Win11DesktopApp.Invoices.Views.InvoicesPlaceholderSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="900">
    <Grid>
        <Border CornerRadius="24"
                BorderBrush="{DynamicResource CardBorderBrush}"
                BorderThickness="1"
                MaxWidth="760"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Padding="26">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#FFF8FBFA" Offset="0"/>
                    <GradientStop Color="#FFF1F8F6" Offset="0.65"/>
                    <GradientStop Color="#FFEDF5F2" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel>
                <Border Width="64"
                        Height="64"
                        CornerRadius="20"
                        Background="#1426A69A"
                        HorizontalAlignment="Center">
                    <TextBlock Text="&#xE946;"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="28"
                               Foreground="{DynamicResource AccentBrush}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="{Binding Title}" FontSize="{DynamicResource FS28}" Style="{StaticResource PageHeaderTitleText}" TextAlignment="Center" Margin="0,18,0,0"/>
                <TextBlock Text="{Binding Description}" FontSize="{DynamicResource FS14}" Foreground="{DynamicResource SecondaryForegroundBrush}"
                           TextAlignment="Center" Margin="0,12,0,0" TextWrapping="Wrap"/>
                <Border Background="{DynamicResource CardBackgroundBrush}"
                        BorderBrush="{DynamicResource CardBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="18"
                        Padding="20"
                        Margin="0,22,0,0">
                    <StackPanel>
                        <TextBlock Text="{Binding Hint}" FontSize="{DynamicResource FS13}" Foreground="{DynamicResource ForegroundBrush}" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
