<Window x:Class="AdminPanel.AdminLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin Login"
        Width="430"
        Height="250"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E2E">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Administrator login"
                   Foreground="#CDD6F4"
                   FontSize="22"
                   FontWeight="SemiBold"/>

        <TextBlock Grid.Row="1"
                   Margin="0,18,0,8"
                   Text="Password"
                   Foreground="#A6ADC8"/>

        <Border Grid.Row="2"
                Height="38"
                Margin="0,0,0,10"
                Background="#313244"
                BorderBrush="#89B4FA"
                BorderThickness="1.5"
                CornerRadius="6">
            <PasswordBox x:Name="PasswordInput"
                         Margin="1"
                         Padding="10,7"
                         Background="#313244"
                         Foreground="#FFFFFF"
                         CaretBrush="#FFFFFF"
                         BorderThickness="0"/>
        </Border>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,22,0,0">
            <Button Width="92"
                    Height="34"
                    Margin="0,0,8,0"
                    Click="Cancel_Click"
                    Content="Cancel"/>
            <Button Width="92"
                    Height="34"
                    Click="Login_Click"
                    Content="Login"/>
        </StackPanel>
    </Grid>
</Window>
